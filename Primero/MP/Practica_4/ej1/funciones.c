#include <stdio.h>
#include <math.h>

float f(float x){
	float resultado = 0;
	resultado = (3*(pow(2.7, x)))-(2*x);
	return resultado;
}

float g(float x){
	float resultado = 0;
	resultado = ((0-x)*sin(x))+1.5;
	return resultado;
}

float z(float x){
	float resultado = 0;
	resultado = (pow(x, 3))-(2*x)+1;
	return resultado;
}